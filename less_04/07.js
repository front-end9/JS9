// Написать функцию, которая принимает от 1 до 5 чисел и возвращает их сумму.

function sum(_one = 0, _two = 0, _three = 0, _four = 0, _five = 0) {
    return _one + _two + _three + _four + _five;
}

alert(sum(1, 1));